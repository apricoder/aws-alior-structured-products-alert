// import { parseBody } from "./event.utils";

describe("Event Utils", () => {

  it('should pass', () => {

  });

  // describe("parseBody", () => {
  //   it("should be defined", () => {
  //     expect(parseBody).toBeDefined();
  //   });
  //
  //   it("should return {} if event body is not present", () => {
  //     expect(parseBody({})).toEqual({});
  //   });
  //
  //   it("should return {} if event body is not string", () => {
  //     expect(parseBody({ body: null })).toEqual({});
  //   });
  //
  //   it("should return parsed body if event body type is string", () => {
  //     expect(parseBody({ body: `{ "hello": "world" }` })).toEqual({
  //       hello: "world",
  //     });
  //   });
  // });
});
